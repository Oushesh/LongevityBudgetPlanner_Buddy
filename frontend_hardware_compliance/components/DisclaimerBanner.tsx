export function DisclaimerBanner() {
  return (
    <div
      role="note"
      className="border-b border-fuchsia-900/40 bg-fuchsia-950/30 px-4 py-2 text-center text-xs text-fuchsia-200"
    >
      Demo only — not legal or regulatory advice. Standards data is curated
      metadata with official links; verify with qualified professionals before
      submission.
    </div>
  );
}
